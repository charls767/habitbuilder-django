# reminders

Reminder creation/edit UI, plus the `flutter_local_notifications` + `timezone` integration
(Android exact-alarm handling, iOS permission flow — the domain's most commonly under-tested area,
see `.planning/research/PITFALLS.md` in the planning workspace).

Jira: epic `HBM-3`, tickets `HBM-13`, `HBM-14`.
