# reminders

Reminder presentation and repository skeleton. Device scheduling uses
`flutter_local_notifications` with `timezone` when HBM-13/HBM-14 are
implemented.

Jira: epic `HBM-3`, tickets `HBM-13`, `HBM-14`.
