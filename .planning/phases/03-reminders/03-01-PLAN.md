---
phase: 03-reminders
plan: "01"
ticket: HBM-13
type: execute
wave: 1
depends_on: []
branch: HBM-13/reminder-ui
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile
files_modified:
  - .planning/phases/03-reminders/03-HBM-13-BASE.json
  - package.json
  - docs/openapi.yaml
  - scripts/mock-smoke.mjs
  - scripts/run-mock-gate.mjs
  - scripts/run-mock-gate.test.mjs
  - scripts/check-changed-coverage.mjs
  - scripts/check-changed-coverage.test.mjs
autonomous: true
requirements: [REMINDER-01, QUALITY-15, QUALITY-16, QUALITY-18]
decisions: [D-01, D-02, D-03, D-07, D-13, D-14, D-15]
must_haves:
  truths:
    - "D-02/D-03/D-07: OpenAPI and Prism expose several reminders per habit with mensaje, strict HH:mm, unique ISO days 1..7 and activo."
    - "QUALITY-15: changed-code coverage fails on missing LCOV paths, zero measurable lines and coverage below 80.00%."
    - "QUALITY-18: one command owns Prism startup, readiness, smoke and exact process-tree cleanup."
    - "D-15: root, branch, start SHA, protected CMake hash and stash object are immutable."
  artifacts:
    - path: ".planning/phases/03-reminders/03-HBM-13-BASE.json"
      provides: "repoRoot, branch, startSha, originMainSha, capturedAtUtc, protectedPath, protectedSha256, stashRef and stashObject"
    - path: "docs/openapi.yaml"
      provides: "Recordatorio/RecordatorioRequest schemas and list/create/edit/delete paths"
    - path: "scripts/run-mock-gate.mjs"
      provides: "readiness-bounded Prism owner with finally cleanup"
    - path: "scripts/check-changed-coverage.mjs"
      provides: "strict changed-Dart LCOV gate"
  key_links:
    - from: "scripts/run-mock-gate.mjs"
      to: "scripts/mock-smoke.mjs"
      via: "spawn only after readiness; terminate captured PID tree in finally"
      pattern: "finally"
    - from: "scripts/mock-smoke.mjs"
      to: "docs/openapi.yaml"
      via: "list/create/edit/deactivate reminder lifecycle"
      pattern: "reminders"
---

<objective>
Freeze HBM-13's baseline and make coverage plus the reminder contract executable
before any Dart feature code.

Purpose: Ensure all later HBM-13 claims use immutable safety evidence and
fail-closed tooling.
Output: Base manifest, strict coverage helper, self-contained Prism gate and
HBB-23-shaped reminder contract.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-CONTEXT.md
@.planning/phases/03-reminders/03-RESEARCH.md
@.planning/phases/03-reminders/03-VALIDATION.md

<branch_guard>
Every command runs from
`C:\Users\USER\Desktop\DPPF\HabitBuilder\habitbuilder-mobile` on
`HBM-13/reminder-ui`. Persist `startSha` before mutation. Before and after each
task assert the root/branch, SHA-256
`C6C0F0C5E484A957961E8653DC9A19A240B799B9FCD7D1120D7FF7D0F2F23AFE`
for `windows/flutter/generated_plugins.cmake`, and stash object
`a6764834f136258ef8c971f6e206f4110e81dee6`. Never stage broadly or mutate the
stash/protected path per D-15.
</branch_guard>
</context>

## Artifacts

- `03-HBM-13-BASE.json`: `repoRoot`, `branch`, `startSha`, `originMainSha`,
  `capturedAtUtc`, `protectedPath`, `protectedSha256`, `stashRef`,
  `stashObject`.
- `check-changed-coverage.mjs`: changed-path discovery, generated exclusions,
  LCOV path matching, measurable-line count, percentage gate and explicit
  fatal diagnostics.
- `run-mock-gate.mjs`: `spawnMock`, readiness polling, smoke child, bounded
  termination and `finally` cleanup; package script `mock:gate`.
- `docs/openapi.yaml`: `Recordatorio`, `RecordatorioRequest`,
  `/habits/{habitId}/reminders` and `/reminders/{reminderId}` operations.
- Tests for missing LCOV, zero lines, threshold boundaries, readiness timeout,
  smoke failure and process cleanup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the immutable baseline and harden changed coverage</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-13/D-15</file>
    <file>scripts/check-changed-coverage.mjs — current CLI and exclusions</file>
    <file>scripts/check-changed-coverage.test.mjs — existing fixture style</file>
    <file>windows/flutter/generated_plugins.cmake — hash only; never edit</file>
  </read_first>
  <files>.planning/phases/03-reminders/03-HBM-13-BASE.json, scripts/check-changed-coverage.mjs, scripts/check-changed-coverage.test.mjs</files>
  <action>Before production edits, assert the branch guard and create the complete base manifest. Per D-13, write failing tests for a changed non-excluded Dart path absent from LCOV, zero measurable lines, below 80.00%, exactly 80.00%, above threshold and excluded generated code. Then make the CLI fail with explicit offending paths for the first three cases. Re-run the guard and commit only these exact paths as `test(HBM-13): harden changed coverage gate`.</action>
  <verify>
    <automated>npm run test:coverage-script &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $b=Get-Content '.planning/phases/03-reminders/03-HBM-13-BASE.json'|ConvertFrom-Json; if((git rev-parse --show-toplevel).Replace('\','/') -ne $b.repoRoot.Replace('\','/')){throw 'root mismatch'}; if((git branch --show-current) -ne $b.branch){throw 'branch mismatch'}; if((Get-FileHash -Algorithm SHA256 $b.protectedPath).Hash -ne $b.protectedSha256){throw 'CMake changed'}; if((git rev-parse 'stash@{0}') -ne $b.stashObject){throw 'stash changed'}"</automated>
  </verify>
  <acceptance_criteria>
    - The manifest captures immutable root/branch/start/safety values before production edits.
    - Every negative coverage fixture exits non-zero for the intended reason.
  </acceptance_criteria>
  <done>The ticket has an immutable base and strict changed-code coverage gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the reminder contract and Prism lifecycle executable</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-02/D-03/D-07</file>
    <file>docs/openapi.yaml — existing reminder schemas and endpoints</file>
    <file>scripts/mock-smoke.mjs — current habits/goals smoke conventions</file>
    <file>package.json — mock scripts</file>
  </read_first>
  <files>package.json, docs/openapi.yaml, scripts/mock-smoke.mjs, scripts/run-mock-gate.mjs, scripts/run-mock-gate.test.mjs</files>
  <action>Correct `Recordatorio` and request schemas per D-02/D-03/D-07 with `mensaje`, strict `HH:mm`, unique/ranged ISO days and `activo`; preserve list/create/edit/delete operations. Extend smoke with list/create/edit/deactivate. Write lifecycle tests before implementation for successful readiness, timeout, child spawn failure, smoke failure and cleanup. Implement a single `mock:gate` owner that starts Prism, stores its PID, accepts any HTTP readiness response, runs smoke, and in `finally` terminates only that process tree and awaits exit. Re-run D-15 guards and commit exact paths as `test(HBM-13): make reminder contract gate self-contained`.</action>
  <verify>
    <automated>node --test scripts/run-mock-gate.test.mjs &amp;&amp; npm run mock:gate &amp;&amp; npm run test:coverage-script &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $p=Get-NetTCPConnection -LocalPort 4010 -State Listen -ErrorAction SilentlyContinue; if($p){throw 'Prism port still owned'}; $b=Get-Content '.planning/phases/03-reminders/03-HBM-13-BASE.json'|ConvertFrom-Json; if((Get-FileHash -Algorithm SHA256 $b.protectedPath).Hash -ne $b.protectedSha256){throw 'CMake changed'}"</automated>
  </verify>
  <acceptance_criteria>
    - Missing LCOV and zero measurable lines each produce non-zero exit with the offending path.
    - `npm run mock:gate` leaves port 4010 free after success and smoke failure.
    - The baseline manifest matches live root, branch, start SHA, stash object and CMake hash.
  </acceptance_criteria>
  <done>The contract and both reusable quality helpers fail closed.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Malformed contract data | mitigate | OpenAPI constraints and Prism smoke |
| False coverage pass | mitigate | Missing-LCOV/zero-lines fatal tests |
| Orphan Prism process | mitigate | Captured PID-tree cleanup in finally |
| Protected state contamination | mitigate | Immutable root/hash/stash guard |
| T-03-SC supply chain | mitigate | No package installation in HBM-13 |
</threat_model>

<verification>Both task verifies pass in the primary root and the protected file is absent from every staged/ticket diff.</verification>
<success_criteria>Baseline, contract, coverage and mock gates are deterministic and fail closed.</success_criteria>
<output>Create `03-01-SUMMARY.md` in the primary root; do not open a PR.</output>
