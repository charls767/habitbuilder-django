---
phase: 03-reminders
plan: "02"
ticket: HBM-13
type: execute
wave: 2
depends_on: ["03-01"]
branch: HBM-13/reminder-ui
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile
files_modified:
  - lib/features/reminders/domain/entities/reminder_time.dart
  - lib/features/reminders/domain/entities/recordatorio.dart
  - lib/features/reminders/domain/repositories/reminder_repository.dart
  - lib/features/reminders/data/models/recordatorio_dto.dart
  - lib/features/reminders/data/datasources/reminder_remote_data_source.dart
  - lib/features/reminders/data/repositories/reminder_repository_impl.dart
  - test/features/reminders/domain/reminder_time_test.dart
  - test/features/reminders/data/recordatorio_dto_test.dart
  - test/features/reminders/data/reminder_remote_data_source_test.dart
  - test/features/reminders/data/reminder_repository_impl_test.dart
autonomous: true
requirements: [REMINDER-01, REMINDER-03, REMINDER-04, QUALITY-13, QUALITY-16]
decisions: [D-02, D-03, D-06, D-07, D-13, D-15]
must_haves:
  truths:
    - "REMINDER-01/D-03: immutable domain/data types represent zero or many reminders per habit."
    - "D-07: message, HH:mm and ISO days are validated before transport."
    - "REMINDER-04: update/toggle round-trips every writable field."
    - "D-06: 409 remains a typed failure for the controller; data never fakes eligibility."
  artifacts:
    - path: "lib/features/reminders/domain/entities/reminder_time.dart"
      provides: "ReminderTime strict wall-clock value"
    - path: "lib/features/reminders/domain/entities/recordatorio.dart"
      provides: "Recordatorio and ReminderDraft"
    - path: "lib/features/reminders/domain/repositories/reminder_repository.dart"
      provides: "list/create/update/delete port"
    - path: "lib/features/reminders/data/datasources/reminder_remote_data_source.dart"
      provides: "exact authenticated Dio calls"
  key_links:
    - from: "reminder_repository_impl.dart"
      to: "reminder_remote_data_source.dart"
      via: "DTO/entity mapping"
      pattern: "ReminderRemoteDataSource"
---

<objective>
Implement HBM-13 domain and data boundaries against the executable reminder
contract.

Purpose: Give CU-006 typed, tested persistence without plugin or UI coupling.
Output: Value objects, entities, repository port, DTO, datasource, adapter and
focused unit tests.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-13-BASE.json
@.planning/phases/03-reminders/03-01-SUMMARY.md
@docs/openapi.yaml

<branch_guard>Load the HBM-13 base before/after every task; assert primary root, exact branch, CMake hash and stash object, use `startSha` for all diffs, and fail if the protected path is staged or in ticket commits per D-15.</branch_guard>
</context>

## Artifacts

- `ReminderTime`: strict `HH:mm`, hour/minute access and canonical string.
- `Recordatorio`: id, habitId, mensaje, hora, diasSemana and activo.
- `ReminderDraft`: full writable message/time/days/active shape.
- `ReminderRepository`: `listByHabit`, `create`, `update`, `delete`.
- `RecordatorioDto`: exact `fromJson`, `toEntity` and request serialization.
- `ReminderRemoteDataSource`: exact list/create/update/delete Dio operations via
  `runApiCall`.
- `ReminderRepositoryImpl`: DTO/entity adaptation with no JSON in presentation.
- Four focused suites covering values, JSON, HTTP and repository mapping.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable reminder domain contracts</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-02/D-03/D-07</file>
    <file>docs/openapi.yaml — finalized Recordatorio shapes</file>
    <file>lib/features/habits/domain/entities/habito.dart — immutable entity conventions</file>
    <file>test/features/habits/domain — assertion style</file>
  </read_first>
  <files>lib/features/reminders/domain/entities/reminder_time.dart, lib/features/reminders/domain/entities/recordatorio.dart, lib/features/reminders/domain/repositories/reminder_repository.dart, test/features/reminders/domain/reminder_time_test.dart</files>
  <action>Write failing tests for `00:00`, `23:59`, malformed times, trimmed non-empty message, unique sorted days and range 1..7. Implement immutable `ReminderTime`, `Recordatorio`, `ReminderDraft` and `ReminderRepository` with list/create/update/delete signatures per D-02/D-03/D-07. Do not import Riverpod, Dio, scheduler packages or plugin types. Run the D-15 guard and commit exact paths as `feat(HBM-13): define reminder domain contracts`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test --no-pub test/features/reminders/domain/reminder_time_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze --no-pub &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $hits=&amp; rg -n 'flutter_local_notifications|package:dio|package:flutter_riverpod' lib/features/reminders/domain; $code=$LASTEXITCODE; if($code -gt 1){exit $code}; if($code -eq 0){$hits; throw 'domain boundary violated'}"</automated>
  </verify>
  <acceptance_criteria>
    - Domain constructors reject every invalid wall-clock/message/day fixture.
    - Repository signatures preserve the complete writable reminder shape.
  </acceptance_criteria>
  <done>The reminder domain is immutable, strict and transport-independent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DTO, Dio datasource and repository adapter</name>
  <read_first>
    <file>lib/features/reminders/domain/entities/recordatorio.dart — contracts from Task 1</file>
    <file>lib/features/habits/data/datasources/habit_remote_data_source.dart — `runApiCall` pattern</file>
    <file>lib/features/habits/data/repositories/habit_repository_impl.dart — mapping pattern</file>
    <file>test/features/habits/data/habit_remote_data_source_test.dart — Dio request assertions</file>
  </read_first>
  <files>lib/features/reminders/data/models/recordatorio_dto.dart, lib/features/reminders/data/datasources/reminder_remote_data_source.dart, lib/features/reminders/data/repositories/reminder_repository_impl.dart, test/features/reminders/data/recordatorio_dto_test.dart, test/features/reminders/data/reminder_remote_data_source_test.dart, test/features/reminders/data/reminder_repository_impl_test.dart</files>
  <action>Write failing tests first for exact JSON, every OpenAPI path/method/payload, 400/404/409 normalization, DTO/entity mapping and full-field activo updates. Implement explicit DTO, authenticated Dio datasource through `runApiCall`, and repository adapter. Per D-06, preserve backend conflict errors for presentation; never add local authorization. Run guards and commit exact paths as `feat(HBM-13): add reminder data layer`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test --no-pub test/features/reminders/data/recordatorio_dto_test.dart test/features/reminders/data/reminder_remote_data_source_test.dart test/features/reminders/data/reminder_repository_impl_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze --no-pub &amp;&amp; npm run mock:gate &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $b=Get-Content '.planning/phases/03-reminders/03-HBM-13-BASE.json'|ConvertFrom-Json; if((Get-FileHash -Algorithm SHA256 $b.protectedPath).Hash -ne $b.protectedSha256){throw 'CMake changed'}; $changed=git diff $($b.startSha)...HEAD --name-only; if($LASTEXITCODE){exit $LASTEXITCODE}; if($changed -contains $b.protectedPath){throw 'protected path in ticket diff'}"</automated>
  </verify>
  <acceptance_criteria>
    - Repository round-trips all four writable fields and never loses configuration on an activo change.
    - Exact endpoints and normalized failures match OpenAPI/Prism.
    - Guard evidence is unchanged and CMake is not staged.
  </acceptance_criteria>
  <done>HBM-13 has a contract-accurate, tested domain/data boundary ready for UI.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Malformed API data | mitigate | Strict DTO/domain constructors |
| Cross-user identifier | transfer | Authenticated backend remains authority |
| False mutation success | mitigate | Typed `runApiCall` failures |
| Plugin coupling | mitigate | Static domain import gate |
| Protected contamination | mitigate | Immutable D-15 manifest |
</threat_model>

<verification>Focused tests, analyze, Prism and D-15 guards all pass with no plugin import.</verification>
<success_criteria>Typed reminder persistence exactly matches the contract and preserves all writable fields.</success_criteria>
<output>Create `03-02-SUMMARY.md` in the primary root; do not open a PR.</output>
