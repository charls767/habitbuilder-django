---
phase: 03-reminders
plan: "06"
ticket: HBM-14
type: execute
wave: 6
depends_on: ["03-05"]
branch: HBM-14/local-notifications
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile-hbm14
files_modified:
  - lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory.dart
  - lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory_native.dart
  - lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory_noop.dart
  - lib/features/reminders/infrastructure/notifications/flutter_notification_gateway.dart
  - lib/features/reminders/infrastructure/notifications/noop_reminder_scheduler.dart
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/drawable/ic_notification.xml
  - android/app/src/main/res/raw/keep.xml
  - ios/Runner/AppDelegate.swift
  - scripts/run-android-reminder-acceptance.ps1
  - integration_test/reminder_android_smoke_test.dart
  - test/features/reminders/infrastructure/notification_permission_policy_test.dart
  - test/features/reminders/infrastructure/noop_reminder_scheduler_test.dart
autonomous: true
requirements: [REMINDER-05, QUALITY-14, QUALITY-16, QUALITY-17]
decisions: [D-09, D-10, D-11, D-13, D-15, D-16]
must_haves:
  truths:
    - "Android permission/exact policy degrades to inexact and receivers restore alarms after reboot."
    - "Pixel_6 acceptance owns startup/readiness/evidence/cleanup and never reports success from unit tests alone."
    - "iOS auto-prompt is disabled, permission is user-context/idempotent and native status remains pending_external on Windows."
    - "Web/desktop use explicit no-op; plugin/cancelAll/local-time scans remain green."
  artifacts:
    - path: "lib/features/reminders/infrastructure/notifications/flutter_notification_gateway.dart"
      provides: "only flutter_local_notifications boundary"
    - path: "lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory.dart"
      provides: "conditional native/no-op factory"
    - path: "scripts/run-android-reminder-acceptance.ps1"
      provides: "Pixel_6 lifecycle and real Android acceptance"
    - path: "ios/Runner/AppDelegate.swift"
      provides: "notification-center delegate preserving implicit engine registration"
  key_links:
    - from: "reminder_scheduler_factory.dart"
      to: "reminder_scheduler_factory_noop.dart"
      via: "conditional export"
      pattern: "dart.library.io"
    - from: "flutter_notification_gateway.dart"
      to: "ReminderScheduler"
      via: "infrastructure adapter only"
      pattern: "ReminderScheduler"
---

<objective>
Implement native and no-op scheduling adapters plus required Android/iOS
configuration in the isolated HBM-14 root.

Purpose: Translate pure scheduling contracts to OS APIs without leaking plugin
types or overstating platform evidence.
Output: Factories, gateway, native configuration, Android runner and policy
tests.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-14-BASE.json
@.planning/phases/03-reminders/03-HBM-14-PACKAGES.json
@C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-05-SUMMARY.md
@lib/features/reminders/domain/services/reminder_scheduler.dart

<guard>Run every command under the isolated root. Before/after each task assert HBM-14 root/branch/startSha and primary CMake/stash. Fail if generated CMake is staged/in the ticket diff. Run `node scripts/check-reminder-boundaries.mjs` in every verify.</guard>
</context>

## Artifacts

- `createReminderScheduler` conditional factory with native and no-op branches.
- `FlutterNotificationGateway`: initialization, permission/exact-status,
  zoned scheduling, pending lookup and targeted managed cancel.
- `NoopReminderScheduler`: explicit unsupported result without plugin/dart:io.
- Android: Java 17/desugaring/multidex, notification/exact/reboot permissions,
  plugin receivers, monochrome icon and keep resource.
- iOS: automatic request flags false, delegate assignment and implicit-engine
  registration preserved.
- Pixel_6 runner/integration smoke and permission-policy/no-op tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Android adapter, configuration and Pixel_6 runner</name>
  <read_first>
    <file>lib/features/reminders/domain/services/reminder_scheduler.dart — pure port</file>
    <file>android/app/build.gradle.kts — current Java/Flutter config</file>
    <file>android/app/src/main/AndroidManifest.xml — current declarations</file>
    <file>.planning/phases/03-reminders/03-RESEARCH.md — Android package requirements</file>
    <file>scripts/check-reminder-boundaries.mjs — mandatory scans</file>
  </read_first>
  <files>lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory.dart, lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory_native.dart, lib/features/reminders/infrastructure/notifications/flutter_notification_gateway.dart, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, android/app/src/main/res/drawable/ic_notification.xml, android/app/src/main/res/raw/keep.xml, scripts/run-android-reminder-acceptance.ps1, integration_test/reminder_android_smoke_test.dart, test/features/reminders/infrastructure/notification_permission_policy_test.dart</files>
  <action>Write fake-boundary policy tests first. Per D-10, schedule one weekly rule per reminder/day, request notification permission only from eligible activation, recheck exact permission during reconciliation, use exact-allow-while-idle only when granted and inexact otherwise. Add required Gradle, manifest, receiver and resource configuration. Build a fail-closed PowerShell runner that locates adb/emulator, starts `Pixel_6` only if needed, waits `sys.boot_completed=1`, runs grant/deny, exact/inexact, edit/deactivate/delete, restart/reboot/package-replace acceptance, captures actual output and stops only its own emulator in `finally`. Commit exact paths as `feat(HBM-14): add Android reminder adapter`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test test/features/reminders/infrastructure/notification_permission_policy_test.dart &amp;&amp; node scripts/check-reminder-boundaries.mjs &amp;&amp; powershell -NoProfile -File scripts/run-android-reminder-acceptance.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Exact granted and denied/inexact paths are tested.
    - Manifest/Gradle/resources match plugin requirements.
    - Pixel_6 runner waits for readiness, captures evidence and cleans only its own emulator.
  </acceptance_criteria>
  <done>Android scheduling and local native acceptance are executable and evidenced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement iOS adapter and explicit web/desktop no-op</name>
  <read_first>
    <file>ios/Runner/AppDelegate.swift — FlutterImplicitEngineDelegate contract</file>
    <file>lib/features/reminders/infrastructure/notifications/flutter_notification_gateway.dart — Android gateway from Task 1</file>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-09/D-11</file>
    <file>scripts/check-reminder-boundaries.mjs — mandatory scans</file>
  </read_first>
  <files>lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory.dart, lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory_native.dart, lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory_noop.dart, lib/features/reminders/infrastructure/notifications/flutter_notification_gateway.dart, lib/features/reminders/infrastructure/notifications/noop_reminder_scheduler.dart, ios/Runner/AppDelegate.swift, test/features/reminders/infrastructure/notification_permission_policy_test.dart, test/features/reminders/infrastructure/noop_reminder_scheduler_test.dart</files>
  <action>Write tests first. Initialize iOS with all automatic request flags false, request alert/sound once from eligible user action, schedule planner-selected one-shots and expose permission state. Preserve implicit-engine registration while assigning `UNUserNotificationCenter` delegate. Implement conditional web/unsupported-desktop no-op without plugin/dart:io imports outside infrastructure. Keep iOS acceptance explicitly `pending_external` on this Windows host; do not create or promise native iOS PASS evidence. Commit exact paths as `feat(HBM-14): add iOS and no-op reminder adapters`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test test/features/reminders/infrastructure/notification_permission_policy_test.dart test/features/reminders/infrastructure/noop_reminder_scheduler_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat build web --release &amp;&amp; node scripts/check-reminder-boundaries.mjs &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; if(-not (Select-String -LiteralPath 'ios/Runner/AppDelegate.swift' -Pattern 'UNUserNotificationCenter')){throw 'iOS delegate missing'}"</automated>
  </verify>
  <acceptance_criteria>
    - iOS permission request is delayed/idempotent and 64 limit remains planner-owned.
    - Web release succeeds through no-op without plugin import.
    - iOS native status is not marked passed on this Windows host.
  </acceptance_criteria>
  <done>Native/no-op adapters are complete with honest platform evidence boundaries.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Permission coercion | mitigate | User-context request and denial state |
| Unrelated cancellation | mitigate | Managed IDs/payload only; cancelAll scan |
| Reboot loss | mitigate | Plugin receivers plus reconciliation |
| False iOS evidence | mitigate | Windows status fixed to pending_external |
| Wrong worktree | mitigate | Root/branch/base checks |
</threat_model>

<verification>Android real runner, web build and all three boundary scans pass; no iOS PASS is claimed.</verification>
<success_criteria>Adapters/config satisfy D-09..11 while preserving isolation and truthful evidence.</success_criteria>
<output>Write and locally version `03-06-SUMMARY.md` from the primary root; record Android evidence and iOS `pending_external`; do not push metadata.</output>
