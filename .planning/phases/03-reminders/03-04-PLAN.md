---
phase: 03-reminders
plan: "04"
ticket: HBM-13
type: execute
wave: 4
depends_on: ["03-03"]
branch: HBM-13/reminder-ui
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile
files_modified:
  - .planning/phases/03-reminders/03-HBM-13-HANDOFF.json
  - .planning/phases/03-reminders/03-HBB-23-HANDOFF.md
  - .planning/phases/03-reminders/03-04-SUMMARY.md
autonomous: true
requirements: [QUALITY-13, QUALITY-15, QUALITY-16, QUALITY-17, QUALITY-18, DELIVERY-07]
decisions: [D-01, D-02, D-13, D-14, D-15]
user_setup:
  - service: bitbucket
    why: "Create, inspect and merge the single HBM-13 pull request"
    env_vars:
      - name: BITBUCKET_ACCESS_TOKEN
        source: "Bitbucket token with pullrequest write scope"
must_haves:
  truths:
    - "`productTipSha` is captured after the last ticket commit and pushed with an exact SHA refspec."
    - "The sole remote HBM-13 PR source commit equals `productTipSha` exactly."
    - "Merge strategy preserves ancestry and `productTipSha` is an ancestor of fetched `origin/main`."
    - "Post-merge SUMMARY/handoffs are local metadata commits on primary-root `main`, recorded as `metadataTipSha` and never pushed."
  artifacts:
    - path: ".planning/phases/03-reminders/03-HBM-13-HANDOFF.json"
      provides: "product/remote/PR/merge/ancestry/metadata SHA separation"
    - path: ".planning/phases/03-reminders/03-HBB-23-HANDOFF.md"
      provides: "frontend-first final contract and downstream order"
    - path: ".planning/phases/03-reminders/03-04-SUMMARY.md"
      provides: "post-merge HBM-13 evidence visible in primary root"
  key_links:
    - from: "productTipSha"
      to: "refs/heads/HBM-13/reminder-ui"
      via: "git push origin <sha>:refs/heads/HBM-13/reminder-ui"
      pattern: "productTipSha"
    - from: "productTipSha"
      to: "origin/main"
      via: "merge-base --is-ancestor after merge/fetch"
      pattern: "ancestry"
---

<objective>
Validate, publish and merge HBM-13 at an exact product tip, then version
post-merge metadata locally from the primary root.

Purpose: Prevent SUMMARY/handoff commits from changing the PR source commit
while preserving orchestration-visible evidence.
Output: Green gates, one merged PR, ancestry proof, HBB-23 handoff and separate
product/metadata SHA records.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-13-BASE.json
@.planning/phases/03-reminders/03-01-SUMMARY.md
@.planning/phases/03-reminders/03-02-SUMMARY.md
@.planning/phases/03-reminders/03-03-SUMMARY.md
@.planning/phases/03-reminders/03-VALIDATION.md

<delivery_contract>
Use Bitbucket REST base
`https://api.bitbucket.org/2.0/repositories/habit_builder/habitbuilder-mobile`.
After all product/tool/test commits, set `productTipSha=git rev-parse HEAD`.
Push exactly
`git push origin "$productTipSha`:refs/heads/HBM-13/reminder-ui"` and assert
`git ls-remote` plus Bitbucket `source.commit.hash` equal it. Use merge strategy
`merge_commit`, fetch, and require ancestry. Only after merge may the primary
root switch to local `main`, write handoff/SUMMARY, commit those exact metadata
paths, record `metadataTipSha`, and assert the remote branch still equals
`productTipSha`. Never push metadata.
</delivery_contract>
</context>

## Artifacts

- Handoff JSON fields: `ticket`, `primaryRoot`, `branch`, `startSha`,
  `productTipSha`, `remoteProductTipSha`, `productPushedAtUtc`, `prId`, `prUrl`,
  `prState`, `destinationBranch`, `destinationShaAtOpen`, `mergeCommit`,
  `mergedAtUtc`, `fetchedOriginMainSha`, `ancestry`, `metadataTipSha`,
  `metadataCommittedAtUtc`, `metadataRecordSha`,
  `metadataPublished=false`, D-15 hashes.
- HBB-23 handoff: final schema/operations, payload fields, frontend
  `productTipSha`, downstream HBB-23 → HBB-24 → HBB-27 and explicit
  not-yet-converged status.
- Summary: actual gates, coverage, PR URL/state, product/merge/main/metadata
  SHAs and no guessed future values.

<tasks>

<task type="auto">
  <name>Task 1: Validate and open exactly one PR at the HBM-13 product tip</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-HBM-13-BASE.json — immutable base</file>
    <file>.planning/phases/03-reminders/03-VALIDATION.md — exact delivery gates</file>
    <file>scripts/check-changed-coverage.mjs — immutable-base CLI</file>
    <file>scripts/run-mock-gate.mjs — self-contained Prism owner</file>
  </read_first>
  <files>None — read-only gates, exact push, Bitbucket PR and merge</files>
  <action>Assert primary root/branch/stash/CMake and use persisted `startSha`, never moving main, for every diff and coverage gate. Run all HBM-13 tests, analyze, full coverage, changed coverage, web, Prism, diff and protected-path checks. Commit any needed product/test/tooling correction before capture. Then set `productTipSha` once, make no later HBM-13 branch commit, push the exact SHA refspec per D-14, assert remote branch equality, query all Bitbucket states and require zero or one matching PR to `main`. Create it only if count is zero; fail on count greater than one or a mismatched existing PR. Require exactly one OPEN PR whose source commit equals `productTipSha`; do not merge or write handoff/SUMMARY in this task.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test --no-pub test/features/reminders &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze --no-pub &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat test --coverage --no-pub &amp;&amp; powershell -NoProfile -Command "$b=Get-Content '.planning/phases/03-reminders/03-HBM-13-BASE.json'|ConvertFrom-Json; node scripts/check-changed-coverage.mjs --base $b.startSha --lcov coverage/lcov.info --min 80; if($LASTEXITCODE){exit $LASTEXITCODE}" &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat build web --release --no-pub &amp;&amp; npm run mock:gate &amp;&amp; git diff --check &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $tip=git rev-parse HEAD; if($LASTEXITCODE){exit $LASTEXITCODE}; $remote=((git ls-remote origin 'refs/heads/HBM-13/reminder-ui') -split '\s+')[0]; if($LASTEXITCODE){exit $LASTEXITCODE}; if($remote -ne $tip){throw 'remote does not equal productTipSha'}"</automated>
  </verify>
  <acceptance_criteria>
    - All gates pass against persisted startSha and protected path is absent from branch/staged diff.
    - Captured tip equals remote source commit and no later commit exists.
    - Bitbucket query returns exactly one OPEN PR to main.
  </acceptance_criteria>
  <done>The sole OPEN HBM-13 PR points exactly to productTipSha.</done>
</task>

<task type="auto">
  <name>Task 2: Version post-merge handoffs and SUMMARY only on local primary main</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-01/D-02/D-14/D-15</file>
    <file>.planning/phases/03-reminders/03-HBM-13-BASE.json — start and safety fields</file>
    <file>C:/Users/USER/.codex/get-shit-done/templates/summary.md — summary contract</file>
    <file>docs/openapi.yaml — final frontend reminder contract</file>
  </read_first>
  <files>.planning/phases/03-reminders/03-HBM-13-HANDOFF.json, .planning/phases/03-reminders/03-HBB-23-HANDOFF.md, .planning/phases/03-reminders/03-04-SUMMARY.md</files>
  <action>Per D-01, HBM-14 remains blocked until this task completes. Recover `productTipSha` from the still-unchanged HBM-13 branch and re-query the sole OPEN PR; verify source/destination/source commit. Merge that PR with `merge_commit`, fetch and require ancestry, then require the same PR MERGED with an actual merge commit. In the primary root switch to local `main` and fast-forward to `origin/main` without stash/autostash or CMake changes. Only now write the complete handoff, HBB-23 handoff and post-merge summary. Commit those exact metadata paths locally and capture that first commit as `metadataTipSha`; update handoff/summary with it and commit the record as `metadataRecordSha`. Do not push either local metadata commit. Assert remote HBM-13 still equals `productTipSha` and remote main contains the product merge, not either metadata SHA.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $h=Get-Content '.planning/phases/03-reminders/03-HBM-13-HANDOFF.json'|ConvertFrom-Json; if($h.productTipSha -eq $h.metadataTipSha -or $h.metadataTipSha -eq $h.metadataRecordSha){throw 'SHA roles conflated'}; $remote=((git ls-remote origin 'refs/heads/HBM-13/reminder-ui') -split '\s+')[0]; if($LASTEXITCODE){exit $LASTEXITCODE}; if($remote -ne $h.productTipSha){throw 'remote product branch moved'}; git fetch origin; if($LASTEXITCODE){exit $LASTEXITCODE}; git merge-base --is-ancestor $h.productTipSha origin/main; if($LASTEXITCODE){throw 'HBM-13 tip not merged'}; if(-not $h.ancestry -or $h.metadataPublished){throw 'handoff flags invalid'}; if((git branch --show-current) -ne 'main'){throw 'metadata not on primary main'}"</automated>
  </verify>
  <acceptance_criteria>
    - Same sole PR is MERGED and its source commit still equals captured tip.
    - Captured tip is an ancestor of fetched origin/main and actual merge commit is persisted.
    - Summary is timestamped after merge and contains no guessed SHA.
  </acceptance_criteria>
  <done>HBM-13 has separate verifiable product and local metadata histories visible to the orchestrator.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Moving coverage base | mitigate | Persisted startSha only |
| Wrong/duplicate PR | mitigate | All-state exact-count and field checks |
| Metadata changes remote tip | mitigate | Exact SHA refspec then local-only main commit |
| Squash loses ancestry | mitigate | Explicit merge_commit strategy |
| Protected contamination | mitigate | D-15 guard before/after |
</threat_model>

<verification>Remote HBM-13 equals `productTipSha`; that SHA is in `origin/main`; local `metadataTipSha` is distinct and unpublished.</verification>
<success_criteria>HBM-13 is merged through exactly one PR and its post-merge metadata is truthful and locally versioned.</success_criteria>
<output>Create `03-04-SUMMARY.md` only after merge/ancestry, from the primary root.</output>
