---
phase: 03-reminders
plan: "08"
ticket: HBM-14
type: execute
wave: 8
depends_on: ["03-07"]
branch: HBM-14/local-notifications
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile-hbm14
files_modified:
  - .planning/phases/03-reminders/03-IOS-ACCEPTANCE.md
  - .planning/phases/03-reminders/03-HBM-14-HANDOFF.json
  - .planning/phases/03-reminders/03-HBB-23-HANDOFF.md
  - .planning/phases/03-reminders/03-08-SUMMARY.md
autonomous: true
requirements: [QUALITY-14, QUALITY-15, QUALITY-16, QUALITY-17, QUALITY-18, DELIVERY-08]
decisions: [D-01, D-02, D-08, D-09, D-10, D-11, D-13, D-14, D-15, D-16]
user_setup:
  - service: bitbucket
    why: "Create, inspect and merge the single HBM-14 pull request"
    env_vars:
      - name: BITBUCKET_ACCESS_TOKEN
        source: "Bitbucket token with pullrequest write scope"
must_haves:
  truths:
    - "All HBM-14 gates and three boundary scans pass against immutable startSha before productTipSha capture."
    - "Remote HBM-14 and the sole PR source commit equal productTipSha exactly."
    - "Merge strategy preserves productTipSha ancestry in fetched origin/main."
    - "iOS remains pending_external with no invented native evidence."
    - "Post-merge handoff/SUMMARY commits are local-only on primary main and distinct from productTipSha."
  artifacts:
    - path: ".planning/phases/03-reminders/03-IOS-ACCEPTANCE.md"
      provides: "Windows-host pending_external record and exact external command requirements"
    - path: ".planning/phases/03-reminders/03-HBM-14-HANDOFF.json"
      provides: "worktree/product/PR/merge/ancestry/metadata separation"
    - path: ".planning/phases/03-reminders/03-08-SUMMARY.md"
      provides: "post-merge Phase 3 evidence visible in primary root"
    - path: ".planning/phases/03-reminders/03-HBB-23-HANDOFF.md"
      provides: "final mobile handoff without false backend convergence"
  key_links:
    - from: "productTipSha"
      to: "refs/heads/HBM-14/local-notifications"
      via: "exact SHA refspec"
      pattern: "productTipSha"
    - from: "productTipSha"
      to: "origin/main"
      via: "merge_commit and ancestry assertion"
      pattern: "ancestry"
---

<objective>
Run final HBM-14 gates, publish and merge one exact product tip, then record
truthful local metadata from the primary root.

Purpose: Complete Phase 3 without moving the PR source tip or fabricating iOS
evidence.
Output: Green gates, one merged PR, ancestry proof, pending iOS record and
separate product/metadata histories.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-14-BASE.json
@.planning/phases/03-reminders/03-HBM-14-PACKAGES.json
@C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-07-SUMMARY.md
@.planning/phases/03-reminders/03-VALIDATION.md

<delivery_contract>
Run product gates/push/PR from the isolated root. Capture `productTipSha` after
the last product/test/tool commit, then make no branch commit. Push exact SHA to
`refs/heads/HBM-14/local-notifications`; require remote and Bitbucket source
commit equality. Merge the same sole PR with `merge_commit`, fetch and require
ancestry. Only after merge, use the primary root's existing local `main`,
integrate `origin/main` without stash/autostash, and locally commit
SUMMARY/handoffs. Never push metadata.
</delivery_contract>
</context>

## Artifacts

- Handoff fields: `ticket`, `primaryRoot`, `worktreeRoot`, `branch`,
  `startSha`, `hbm13ProductTipSha`, `productTipSha`, `remoteProductTipSha`,
  package-evidence hash, gate results, Android record, `prId`, `prUrl`,
  `prState`, destination, `mergeCommit`, `mergedAtUtc`,
  `fetchedOriginMainSha`, `ancestry`, `metadataTipSha`,
  `metadataRecordSha`, `metadataPublished=false`, D-15 values.
- iOS record: `status: pending_external`, host OS, unavailable Xcode reason and
  exact macOS/CI commands/evidence fields required for a later independent run;
  no PASS.
- Final summary: actual product/merge/main/metadata SHAs, package helper result,
  coverage/gates, Android evidence and iOS pending status.
- HBB-23 update: final mobile product tip and downstream convergence still not
  claimed.

<tasks>

<task type="auto">
  <name>Task 1: Validate and open exactly one PR at HBM-14 productTipSha</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-HBM-14-BASE.json — immutable base/worktree</file>
    <file>.planning/phases/03-reminders/03-HBM-14-PACKAGES.json — D-16 evidence</file>
    <file>scripts/check-reminder-boundaries.mjs — plugin/cancelAll/local-time scans</file>
    <file>scripts/run-android-reminder-acceptance.ps1 — real Android gate</file>
    <file>scripts/check-changed-coverage.mjs — immutable-base coverage</file>
  </read_first>
  <files>None — final gates, exact push and Bitbucket OPEN PR only</files>
  <action>Under the isolated root, revalidate D-15 and D-16 post-gate. Run focused/full tests, analyze, strict changed coverage against HBM-14 `startSha`, web, Prism, Android Pixel_6, boundary scans, payload-literal scan, diff and protected-path exclusion. Commit any final product/test correction before capture. Set `productTipSha` once and make no later HBM-14 commit. Push the exact SHA refspec, assert remote equality, query all Bitbucket states and require zero or one matching PR; create one titled `HBM-14: Add timezone-aware local notifications` only when zero, then require exactly one OPEN PR to main containing HBM-13 whose source commit equals `productTipSha`. Do not write iOS/handoff/SUMMARY metadata yet.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test --coverage &amp;&amp; powershell -NoProfile -Command "$b=Get-Content '.planning/phases/03-reminders/03-HBM-14-BASE.json'|ConvertFrom-Json; node scripts/check-changed-coverage.mjs --base $b.startSha --lcov coverage/lcov.info --min 80; if($LASTEXITCODE){exit $LASTEXITCODE}" &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat build web --release &amp;&amp; npm run mock:gate &amp;&amp; node scripts/verify-reminder-package-gate.mjs post --evidence .planning/phases/03-reminders/03-HBM-14-PACKAGES.json --pubspec pubspec.yaml --lockfile pubspec.lock &amp;&amp; node scripts/check-reminder-boundaries.mjs &amp;&amp; powershell -NoProfile -File scripts/run-android-reminder-acceptance.ps1 &amp;&amp; git diff --check &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $tip=git rev-parse HEAD; if($LASTEXITCODE){exit $LASTEXITCODE}; $remote=((git ls-remote origin 'refs/heads/HBM-14/local-notifications') -split '\s+')[0]; if($LASTEXITCODE){exit $LASTEXITCODE}; if($remote -ne $tip){throw 'remote does not equal productTipSha'}"</automated>
  </verify>
  <acceptance_criteria>
    - All automated gates pass against startSha and CMake is absent from branch/staged diff.
    - Android evidence is real; iOS status accurately reflects attached or missing external evidence.
    - Exactly one OPEN HBM-14 PR has captured source tip and main destination.
  </acceptance_criteria>
  <done>HBM-14 has immutable green evidence and one OPEN PR at exactly productTipSha.</done>
</task>

<task type="auto">
  <name>Task 2: Merge with ancestry and version post-merge metadata in the primary root</name>
  <read_first>
    <file>C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-HBM-13-HANDOFF.json — upstream product tip</file>
    <file>.planning/phases/03-reminders/03-HBM-14-BASE.json — root/start safety</file>
    <file>C:/Users/USER/.codex/get-shit-done/templates/summary.md — final summary contract</file>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-02/D-11/D-14/D-15</file>
  </read_first>
  <files>.planning/phases/03-reminders/03-IOS-ACCEPTANCE.md, .planning/phases/03-reminders/03-HBM-14-HANDOFF.json, .planning/phases/03-reminders/03-HBB-23-HANDOFF.md, .planning/phases/03-reminders/03-08-SUMMARY.md</files>
  <action>Re-query the same sole OPEN PR and revalidate source/destination/source commit. Merge with `merge_commit`, fetch and require isolated `productTipSha` ancestor of `origin/main`; require the same PR MERGED. In the primary root, fetch and merge `origin/main` into local `main` without stash/autostash, preserving prior local metadata commits. Write iOS status strictly `pending_external` because this Windows execution has no Xcode evidence. Write handoff, HBB-23 update and final summary with actual values. Commit exact metadata paths locally, capture that first commit as `metadataTipSha`, then update handoff/summary with it and commit that record as `metadataRecordSha`; neither commit is pushed. Assert both remote product branches still equal their product tips, metadataPublished=false, primary CMake/stash unchanged and HBB-23 remains downstream rather than falsely complete.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $primary='C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile'; $h=Get-Content ""$primary/.planning/phases/03-reminders/03-HBM-14-HANDOFF.json""|ConvertFrom-Json; git fetch origin; if($LASTEXITCODE){exit $LASTEXITCODE}; git merge-base --is-ancestor $h.productTipSha origin/main; if($LASTEXITCODE){throw 'HBM-14 product tip not merged'}; $remote=((git ls-remote origin 'refs/heads/HBM-14/local-notifications') -split '\s+')[0]; if($LASTEXITCODE){exit $LASTEXITCODE}; if($remote -ne $h.productTipSha){throw 'remote product branch moved'}; if($h.productTipSha -eq $h.metadataTipSha -or $h.metadataTipSha -eq $h.metadataRecordSha){throw 'SHA roles conflated'}; if($h.metadataPublished -or -not $h.ancestry){throw 'handoff flags invalid'}; $ios=Get-Content ""$primary/.planning/phases/03-reminders/03-IOS-ACCEPTANCE.md"" -Raw; if($ios -notmatch 'pending_external' -or $ios -match '(?im)^status:\s*pass'){throw 'iOS evidence overstated'}; node scripts/check-reminder-boundaries.mjs; if($LASTEXITCODE){exit $LASTEXITCODE}"</automated>
  </verify>
  <acceptance_criteria>
    - Same sole PR is MERGED and merge/tip/main SHAs are actual values.
    - Final summary is post-merge and does not overstate iOS or HBB-23 status.
    - Original stash/CMake and generated-file exclusion remain exact.
  </acceptance_criteria>
  <done>HBM-14 is verifiably merged and all post-merge metadata is local, visible and truthful.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Moving product source | mitigate | Exact SHA push and no post-tip branch commits |
| Wrong/duplicate PR | mitigate | All-state exact-count/source checks |
| Squash loses ancestry | mitigate | merge_commit strategy |
| False iOS evidence | mitigate | Mandatory pending_external record |
| Metadata publication | mitigate | Primary-root local commits plus remote-tip assertions |
| Boundary regression | mitigate | Tested plugin/cancelAll/local-time scan |
</threat_model>

<verification>Remote HBM-14 equals productTipSha; it is in origin/main; local metadata SHAs are distinct/unpublished; iOS remains pending_external.</verification>
<success_criteria>All 13 requirements are satisfied through exactly two product branches/PRs and eight sequential plans.</success_criteria>
<output>Create `03-08-SUMMARY.md` only after merge/ancestry, from the primary root; never push metadata commits.</output>
