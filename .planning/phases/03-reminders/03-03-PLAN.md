---
phase: 03-reminders
plan: "03"
ticket: HBM-13
type: execute
wave: 3
depends_on: ["03-02"]
branch: HBM-13/reminder-ui
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile
files_modified:
  - lib/features/reminders/presentation/providers/reminder_providers.dart
  - lib/features/reminders/presentation/screens/reminders_screen.dart
  - lib/features/reminders/presentation/widgets/reminder_card.dart
  - lib/features/reminders/presentation/widgets/reminder_form_sheet.dart
  - lib/features/habits/presentation/screens/habits_list_screen.dart
  - lib/core/router/app_routes.dart
  - lib/core/router/app_router.dart
  - test/features/reminders/presentation/reminder_controller_test.dart
  - test/features/reminders/presentation/reminders_screen_test.dart
  - test/app_test.dart
autonomous: true
requirements: [REMINDER-01, REMINDER-03, REMINDER-04, QUALITY-13, QUALITY-16, QUALITY-17]
decisions: [D-04, D-05, D-06, D-07, D-13, D-14, D-15]
must_haves:
  truths:
    - "D-04/D-05: a habit action reaches protected `/habits/:habitId/reminders` and CU-006 remains usable at 320 px."
    - "D-06: UI and controller block create/reactivate for paused/completed habits while edit/deactivate remain available."
    - "D-07: validation/API failures preserve entered fields and toggles never claim false success."
  artifacts:
    - path: "lib/features/reminders/presentation/providers/reminder_providers.dart"
      provides: "repository/list providers and ReminderController"
    - path: "lib/features/reminders/presentation/screens/reminders_screen.dart"
      provides: "CU-006 loading/empty/error/list and degraded states"
    - path: "lib/features/reminders/presentation/widgets/reminder_form_sheet.dart"
      provides: "create/edit form with preserved state"
    - path: "test/features/reminders/presentation/reminders_screen_test.dart"
      provides: "widget, semantics and 320 px evidence"
  key_links:
    - from: "habits_list_screen.dart"
      to: "app_routes.dart"
      via: "AppRoutes.habitReminders(habitId)"
      pattern: "habitReminders"
    - from: "reminder_providers.dart"
      to: "habit_providers.dart"
      via: "habitDetailProvider eligibility"
      pattern: "habitDetailProvider"
---

<objective>
Deliver HBM-13 CU-006 and controller behavior on the typed reminder boundary.

Purpose: Make reminder management reachable, resilient and non-bypassably
eligible.
Output: Protected route, habit action, Riverpod controller, responsive screen,
widgets and tests.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-13-BASE.json
@.planning/phases/03-reminders/03-02-SUMMARY.md
@lib/features/reminders/domain/entities/recordatorio.dart
@lib/features/reminders/domain/repositories/reminder_repository.dart

<branch_guard>Run only in the primary root on HBM-13; load the base before/after each task, use `startSha`, and fail on branch/root/stash/CMake drift or protected staging per D-15.</branch_guard>
</context>

## Artifacts

- Providers: `reminderRemoteDataSourceProvider`, `reminderRepositoryProvider`,
  `remindersListProvider(habitId)` and `ReminderController`.
- Controller operations: create, update, toggle and delete with habit
  eligibility and scoped invalidation.
- Routing: `AppRoutes.habitReminders(String habitId)` and protected GoRoute.
- UI: `RemindersScreen`, `ReminderCard`, `ReminderFormSheet`, green summary,
  state views, day chips, switch, add action and delete confirmation.
- Tests: controller zero-call eligibility, route/habit action, all CRUD states,
  failure preservation, semantics, duplicate-submit and 320x640 layout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver the route-to-create CU-006 tracer</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-04/D-05/D-06</file>
    <file>lib/features/habits/presentation/providers/habit_providers.dart — family/detail and mutation patterns</file>
    <file>lib/core/router/app_routes.dart — route helper conventions</file>
    <file>lib/core/router/app_router.dart — authenticated GoRoute conventions</file>
    <file>lib/core/widgets/app_chrome.dart — AppContent/AppSectionLabel</file>
  </read_first>
  <files>lib/features/reminders/presentation/providers/reminder_providers.dart, lib/features/reminders/presentation/screens/reminders_screen.dart, lib/features/habits/presentation/screens/habits_list_screen.dart, lib/core/router/app_routes.dart, lib/core/router/app_router.dart, test/features/reminders/presentation/reminder_controller_test.dart, test/features/reminders/presentation/reminders_screen_test.dart, test/app_test.dart</files>
  <action>Write failing controller/router/widget tests first. Per D-04/D-05, add `AppRoutes.habitReminders`, its protected route, the habit action and the CU-006 route-to-create tracer. Add family providers and `ReminderController`; per D-06, query `habitDetailProvider` and reject ineligible create before repository calls. Implement production loading/empty/error/retry/list/create states, preserve values after local/API failures and invalidate only that habit's reminder list after success. Generate ignored Riverpod code, guard D-15 and commit exact paths as `feat(HBM-13): add reminder UI tracer`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\cache\dart-sdk\bin\dart.exe run build_runner build --delete-conflicting-outputs &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat test --no-pub test/features/reminders/presentation/reminder_controller_test.dart test/features/reminders/presentation/reminders_screen_test.dart test/app_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze --no-pub &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; if((git branch --show-current) -ne 'HBM-13/reminder-ui'){throw 'wrong branch'}"</automated>
  </verify>
  <acceptance_criteria>
    - Route/habit action reaches the correct habit-scoped screen.
    - Active habit creates; paused/completed attempts make zero repository calls.
    - Loading, empty, error and successful create are automated.
  </acceptance_criteria>
  <done>The user can reach CU-006 and create a valid reminder end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete edit, toggle, delete and responsive CU-006 states</name>
  <read_first>
    <file>C:/Users/USER/Desktop/DPPF/HabitBuilder - Mockups.html — CU-006 frame</file>
    <file>lib/features/reminders/presentation/screens/reminders_screen.dart — tracer from Task 1</file>
    <file>lib/features/habits/domain/entities/habito.dart — HabitoEstado values</file>
    <file>test/features/reminders/presentation/reminders_screen_test.dart — tracer fixtures</file>
  </read_first>
  <files>lib/features/reminders/presentation/providers/reminder_providers.dart, lib/features/reminders/presentation/screens/reminders_screen.dart, lib/features/reminders/presentation/widgets/reminder_card.dart, lib/features/reminders/presentation/widgets/reminder_form_sheet.dart, test/features/reminders/presentation/reminder_controller_test.dart, test/features/reminders/presentation/reminders_screen_test.dart</files>
  <action>Write failing tests then complete the CU-006 summary, prominent time/message/day chips, per-entry switch, add action and overflow delete. Validate message/time/day; hydrate edit exactly once; submit the full writable shape; rollback failed switches; block reactivation for ineligible habits with D-06 copy while allowing edit/deactivate/delete; remove only after successful delete. Test 320x640 overflow, semantics and duplicate-submit prevention. Guard D-15 and commit exact paths as `feat(HBM-13): complete CU-006 reminder management`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test --no-pub test/features/reminders/presentation/reminder_controller_test.dart test/features/reminders/presentation/reminders_screen_test.dart &amp;&amp; C:\Users\USER\flutter\bin\cache\dart-sdk\bin\dart.exe format --output=none --set-exit-if-changed lib/features/reminders lib/features/habits/presentation/screens/habits_list_screen.dart lib/core/router test/features/reminders test/app_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze --no-pub &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $b=Get-Content '.planning/phases/03-reminders/03-HBM-13-BASE.json'|ConvertFrom-Json; $staged=git diff --cached --name-only; if($LASTEXITCODE){exit $LASTEXITCODE}; if($staged -contains $b.protectedPath){throw 'CMake staged'}"</automated>
  </verify>
  <acceptance_criteria>
    - Create/edit/toggle/delete success and failure paths preserve authoritative state.
    - Paused/completed explanation and allowed operations match D-06.
    - 320 px test has no overflow and all controls are accessible.
  </acceptance_criteria>
  <done>All HBM-13 visible behavior is complete and ready for delivery gates.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Eligibility bypass | mitigate | Controller and UI share predicate |
| False mutation success | mitigate | State changes only after repository success |
| Duplicate writes | mitigate | Loading/submit lock |
| Cross-user IDs | transfer | Backend ownership enforcement |
| Protected contamination | mitigate | D-15 checks per task |
</threat_model>

<verification>Focused tests, codegen, format and analyze pass; no PR is opened.</verification>
<success_criteria>CU-006 is reachable, complete, responsive and eligibility-safe.</success_criteria>
<output>Create `03-03-SUMMARY.md` in the primary root; 03-04 owns publication and merge.</output>
