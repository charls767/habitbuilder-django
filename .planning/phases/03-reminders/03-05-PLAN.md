---
phase: 03-reminders
plan: "05"
ticket: HBM-14
type: execute
wave: 5
depends_on: ["03-04"]
branch: HBM-14/local-notifications
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile-hbm14
files_modified:
  - .planning/phases/03-reminders/03-HBM-14-BASE.json
  - .planning/phases/03-reminders/03-HBM-14-PACKAGES.json
  - scripts/verify-reminder-package-gate.mjs
  - scripts/verify-reminder-package-gate.test.mjs
  - scripts/check-reminder-boundaries.mjs
  - scripts/check-reminder-boundaries.test.mjs
  - pubspec.yaml
  - pubspec.lock
  - lib/features/reminders/domain/services/managed_notification.dart
  - lib/features/reminders/domain/services/reminder_occurrence_planner.dart
  - lib/features/reminders/domain/services/reminder_scheduler.dart
  - test/features/reminders/domain/reminder_occurrence_planner_test.dart
  - test/features/reminders/domain/ios_occurrence_allocator_test.dart
  - test/features/reminders/domain/managed_notification_id_test.dart
autonomous: true
requirements: [REMINDER-02, REMINDER-05, QUALITY-14, QUALITY-15, QUALITY-17, DELIVERY-08]
decisions: [D-01, D-08, D-09, D-11, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "The manual bootstrap creates HBM-14 from fetched origin/main containing HBM-13 productTipSha and every plan 03-05..08 runs under the exact isolated root."
    - "D-16 pre/post helper tests and evidence prove exact official metadata and UTC ordering around pubspec/lock mutation."
    - "D-08 planner uses injected TZDateTime/profile Location and exact gap/overlap policy."
    - "The literal managed payload prefix is exactly `habit-reminder:v1:` everywhere."
    - "Boundary scans fail on plugin imports outside infrastructure, any cancelAll call, or local-time scheduling inputs."
  artifacts:
    - path: ".planning/phases/03-reminders/03-HBM-14-BASE.json"
      provides: "primary/worktree roots, branch, startSha, originMainSha, HBM-13 product tip and D-15 values"
    - path: "scripts/verify-reminder-package-gate.mjs"
      provides: "pre/post D-16 metadata and mutation-order CLI"
    - path: "scripts/check-reminder-boundaries.mjs"
      provides: "fail-closed plugin/cancelAll/local-DateTime scans"
    - path: "lib/features/reminders/domain/services/reminder_occurrence_planner.dart"
      provides: "pure occurrence and iOS earliest-64 planning"
  key_links:
    - from: "03-HBM-13-HANDOFF.json"
      to: "03-HBM-14-BASE.json"
      via: "ancestry against origin/main before worktree bootstrap"
      pattern: "productTipSha"
    - from: "03-HBM-14-PACKAGES.json"
      to: "pubspec.lock"
      via: "pre/post helper temporal/hash validation"
      pattern: "checkedAtUtc"
---

<objective>
Bootstrap the manually selected HBM-14 worktree, enforce D-16 before and after
dependency mutation, and implement pure scheduling contracts.

Purpose: Freeze worktree, supply-chain and temporal semantics before native
integration.
Output: Isolated base, tested package/boundary helpers, exact pins and pure
timezone planner/port.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-HBM-13-HANDOFF.json
@C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-04-SUMMARY.md
@.planning/phases/03-reminders/03-CONTEXT.md
@.planning/phases/03-reminders/03-RESEARCH.md

<manual_worktree_protocol>
`workflow.use_worktrees=false`; no orchestrator/subagent may auto-create a
worktree. From the primary root, fetch origin and read the HBM-13 handoff;
require its `productTipSha` ancestor of `origin/main`. Fail if the isolated path,
local HBM-14 branch, or remote HBM-14 branch already exists unexpectedly. Run
`git worktree add -b HBM-14/local-notifications
C:\Users\USER\Desktop\DPPF\HabitBuilder\habitbuilder-mobile-hbm14 origin/main`.
Then launch 03-05 and every 03-06..08 executor with cwd exactly the isolated
root. Each command reasserts root/branch. Summaries are written/versioned by
absolute path in the primary root so the orchestrator sees them; those metadata
commits are never pushed.
</manual_worktree_protocol>
</context>

## Artifacts

- Base fields: `primaryRoot`, `worktreeRoot`, `branch`, `startSha`,
  `originMainSha`, `hbm13ProductTipSha`, `capturedAtUtc`, protected path/hash,
  stash ref/object.
- Package helper commands: `pre --output ...` and `post --evidence ...`; exact
  expected package descriptors and injected-fetch/temp-fixture tests.
- Package evidence: `checkedAtUtc`, `mutationStartedAtUtc`,
  `postValidatedAtUtc`, before/after pubspec/lock hashes and package records.
- Boundary helper: scans for plugin imports in domain/application/presentation/
  tests, `.cancelAll(` anywhere in reminder code, and `DateTime.now`,
  `TZDateTime.local` or `.toLocal()` in scheduling code; spawn/rg errors fail.
- Pure symbols: `ManagedNotification`, `ReminderScheduler`,
  `ReminderOccurrencePlanner`, `IosOccurrenceAllocator`,
  `ManagedNotificationId`; payload `habit-reminder:v1:<habitId>:<reminderId>:<key>`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate the manual worktree and build tested fail-closed helpers</name>
  <read_first>
    <file>C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/config.json — use_worktrees=false</file>
    <file>C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-HBM-13-HANDOFF.json — product tip/ancestry</file>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-08/D-09/D-14/D-15/D-16</file>
    <file>pubspec.yaml — pre-mutation dependency state</file>
    <file>pubspec.lock — pre-mutation resolved state</file>
  </read_first>
  <files>.planning/phases/03-reminders/03-HBM-14-BASE.json, scripts/verify-reminder-package-gate.mjs, scripts/verify-reminder-package-gate.test.mjs, scripts/check-reminder-boundaries.mjs, scripts/check-reminder-boundaries.test.mjs</files>
  <action>Execute and revalidate the manual protocol, then persist the complete HBM-14 base. Write Node tests first. Package-helper fixtures must fail for wrong name/version, HTTP URL, wrong FLN homepage, non-null unexpected FLN repository, wrong timezone repository, non-null unexpected timezone homepage, bad archive URL, stale/non-UTC timestamp, mutation starting before check, unchanged/missing lockfile and wrong lock versions. Boundary-helper fixtures must fail on each forbidden plugin/cancelAll/local-time pattern and on unavailable/erroring `rg`. Implement both CLIs without new dependencies. Per D-16 hard-code exact official version API/archive URLs, FLN homepage and timezone repository. Commit exact helper/base paths as `test(HBM-14): add package and boundary gates`.</action>
  <verify>
    <automated>node --test scripts/verify-reminder-package-gate.test.mjs &amp;&amp; node --test scripts/check-reminder-boundaries.test.mjs &amp;&amp; node scripts/check-reminder-boundaries.mjs &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $b=Get-Content '.planning/phases/03-reminders/03-HBM-14-BASE.json'|ConvertFrom-Json; if((git rev-parse --show-toplevel).Replace('\','/') -ne $b.worktreeRoot.Replace('\','/')){throw 'wrong worktree'}; if((git branch --show-current) -ne 'HBM-14/local-notifications'){throw 'wrong branch'}; git -C $b.primaryRoot merge-base --is-ancestor $b.hbm13ProductTipSha origin/main; if($LASTEXITCODE){throw 'HBM-13 ancestry missing'}"</automated>
  </verify>
  <acceptance_criteria>
    - Worktree root, branch and start SHA equal the manual protocol and fetched origin/main.
    - Every malformed package/boundary fixture exits non-zero.
    - Primary CMake/stash values remain unchanged.
  </acceptance_criteria>
  <done>The isolated root and both reusable fail-closed helpers are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Run D-16 before and after exact package mutation</name>
  <read_first>
    <file>scripts/verify-reminder-package-gate.mjs — pre/post CLI contract</file>
    <file>scripts/verify-reminder-package-gate.test.mjs — exact expected metadata</file>
    <file>pubspec.yaml — dependency insertion point</file>
    <file>pubspec.lock — exact resolution target</file>
  </read_first>
  <files>.planning/phases/03-reminders/03-HBM-14-PACKAGES.json, pubspec.yaml, pubspec.lock</files>
  <action>Run helper `pre` before any pubspec write. It must query `https://pub.dev/api/packages/flutter_local_notifications/versions/22.2.0` and `https://pub.dev/api/packages/timezone/versions/0.11.1`, validate exact names/versions, HTTPS API/archive URLs and the exact field/null URL expectations, then persist UTC and before hashes. Record `mutationStartedAtUtc` through the helper only after `checkedAtUtc`. Pin exactly `flutter_local_notifications: 22.2.0` and `timezone: 0.11.1`, run `flutter pub get`, then run helper `post`; it must prove check &lt; mutation &lt; resulting pubspec/lock mtimes, changed hashes and exact lock versions. Any failure stops before commit. Exclude generated CMake and commit exact paths as `build(HBM-14): pin verified notification dependencies`.</action>
  <verify>
    <automated>node scripts/verify-reminder-package-gate.mjs post --evidence .planning/phases/03-reminders/03-HBM-14-PACKAGES.json --pubspec pubspec.yaml --lockfile pubspec.lock &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat pub get &amp;&amp; node scripts/verify-reminder-package-gate.mjs post --evidence .planning/phases/03-reminders/03-HBM-14-PACKAGES.json --pubspec pubspec.yaml --lockfile pubspec.lock &amp;&amp; node scripts/check-reminder-boundaries.mjs</automated>
  </verify>
  <acceptance_criteria>
    - HBM-13 captured tip is ancestor of fetched main before branch creation.
    - Base manifest records exact isolated root/branch/start SHA and original safety values.
    - Official evidence predates pub get and lockfile pins exactly 22.2.0/0.11.1.
  </acceptance_criteria>
  <done>D-16 evidence surrounds exact dependency mutation and lock resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic timezone planner and scheduling port</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-08/D-09/D-11</file>
    <file>lib/features/reminders/domain/entities/recordatorio.dart — reminder fields</file>
    <file>scripts/check-reminder-boundaries.mjs — forbidden boundaries</file>
    <file>pubspec.lock — exact timezone version</file>
  </read_first>
  <files>lib/features/reminders/domain/services/managed_notification.dart, lib/features/reminders/domain/services/reminder_occurrence_planner.dart, lib/features/reminders/domain/services/reminder_scheduler.dart, test/features/reminders/domain/reminder_occurrence_planner_test.dart, test/features/reminders/domain/ios_occurrence_allocator_test.dart, test/features/reminders/domain/managed_notification_id_test.dart</files>
  <action>Write failing tests for same-day past/future, ISO weekdays, rollover, profile/device zone difference, invalid IANA zone, D-08 gap first-valid, overlap single earliest-offset, deterministic ties, fewer/exactly/more than 64 iOS events, stable non-negative IDs and literal payload prefix `habit-reminder:v1:`. Per D-09/D-11, implement a pure Dart scheduler port, globally earliest-64 allocator and contracts with explicit `tz.Location` plus injected `TZDateTime now`; never use local now, duration-day arithmetic, `hashCode` or plugin types. Commit exact paths as `feat(HBM-14): add deterministic reminder planner port` after boundary and D-15 guards.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test test/features/reminders/domain/reminder_occurrence_planner_test.dart test/features/reminders/domain/ios_occurrence_allocator_test.dart test/features/reminders/domain/managed_notification_id_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze &amp;&amp; node scripts/check-reminder-boundaries.mjs &amp;&amp; powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $hits=&amp; rg -n 'habit-reminder' lib test scripts .planning/phases/03-reminders; $code=$LASTEXITCODE; if($code -gt 1){exit $code}; if($code -eq 1){throw 'payload literal missing'}; $bad=$hits|Where-Object{$_ -notmatch 'habit-reminder:v1:'}; if($bad){$bad; throw 'payload literal drift'}"</automated>
  </verify>
  <acceptance_criteria>
    - Named DST tests prove exact D-08 gap/overlap policy.
    - iOS allocator returns globally earliest at most 64 with deterministic ties.
    - Domain/tests contain no plugin import or local-time decision.
  </acceptance_criteria>
  <done>Pure HBM-14 scheduling semantics are deterministic and adapter-ready.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Wrong/unmerged base | mitigate | Manual root plus ancestry preflight |
| Package substitution | mitigate | Tested D-16 exact pre/post helper |
| DST duplicate/shift | mitigate | Named policy tests |
| Boundary erosion | mitigate | Tested fail-closed scans |
| Original-root mutation | mitigate | Cross-root D-15 assertions |
</threat_model>

<verification>All commands run in the isolated root; package ordering and all three boundary scans pass.</verification>
<success_criteria>HBM-14 has a valid manual worktree, exact packages and pure deterministic planner.</success_criteria>
<output>Write and locally version `03-05-SUMMARY.md` at `C:\Users\USER\Desktop\DPPF\HabitBuilder\habitbuilder-mobile\.planning\phases\03-reminders\03-05-SUMMARY.md`; do not push that metadata commit.</output>
