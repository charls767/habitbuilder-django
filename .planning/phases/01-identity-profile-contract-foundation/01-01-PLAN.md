---
phase: 01-identity-profile-contract-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - analysis_options.yaml
  - bitbucket-pipelines.yml
  - docs/openapi.yaml
  - lib/app.dart
  - lib/main.dart
  - lib/core/config/app_env.dart
  - lib/core/network/api_exception.dart
  - lib/core/network/auth_session_controller.dart
  - lib/core/network/dio_client.dart
  - lib/core/network/jwt_interceptor.dart
  - lib/core/router/app_router.dart
  - lib/core/router/app_routes.dart
  - lib/core/storage/token_storage.dart
  - lib/core/theme/app_theme.dart
  - lib/features/auth/README.md
  - lib/features/goals/README.md
  - lib/features/habits/README.md
  - lib/features/profile/README.md
  - lib/features/progress/README.md
  - lib/features/reminders/README.md
  - lib/features/tracking/README.md
  - package.json
  - package-lock.json
  - pubspec.yaml
  - pubspec.lock
  - scripts/bootstrap.ps1
  - scripts/check-changed-coverage.mjs
  - scripts/check-changed-coverage.test.mjs
  - scripts/mock-smoke.mjs
  - test/core/network/api_exception_test.dart
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - QUALITY-01
  - QUALITY-02
  - QUALITY-03
  - QUALITY-04
  - QUALITY-05
  - QUALITY-06
  - DELIVERY-01
user_setup: []
must_haves:
  truths:
    - "D-01: Cada ticket usa su rama HBM-9 exacta y tiene un PR propio."
    - "D-02: Los commits son atomicos y los PRs se apilan HBM-7 -> HBM-8 -> HBM-9."
    - "D-03: Cada ticket incluye pruebas unitarias y cobertura changed-code >=80%."
    - "D-04: Analyze, test, build y Prism pasan antes del push y PR con evidencia."
    - "D-05: El proyecto usa feature-first, Riverpod, go_router, Dio y flutter_secure_storage."
    - "D-06: OpenAPI alimenta Prism, las zonas son IANA y las features posteriores quedan excluidas."
    - "D-07: El worktree combinado se respalda, restaura por rutas y audita antes de borrar el stash."
  artifacts:
    - path: "docs/openapi.yaml"
      provides: "Contrato ejecutable para autenticacion y perfil"
    - path: "lib/core/network/dio_client.dart"
      provides: "Cliente HTTP compartido"
    - path: "lib/core/storage/token_storage.dart"
      provides: "Persistencia segura de access y refresh tokens"
    - path: "scripts/bootstrap.ps1"
      provides: "Bootstrap reproducible con version de Flutter validada"
    - path: "scripts/check-changed-coverage.mjs"
      provides: "Gate de cobertura sobre archivos cambiados"
  key_links:
    - from: "lib/core/network/jwt_interceptor.dart"
      to: "lib/core/storage/token_storage.dart"
      via: "lectura, refresh y limpieza de tokens"
    - from: "package.json"
      to: "docs/openapi.yaml"
      via: "comandos Prism y smoke"
    - from: "bitbucket-pipelines.yml"
      to: "scripts/bootstrap.ps1"
      via: "verificacion reproducible de CI"
---

<objective>
Entregar HBM-7 como la base tecnica aislada de HabitBuilder Mobile.

Purpose: Crear una plataforma estable para que auth y perfil se desarrollen en
PRs posteriores sin depender del backend real.

Output: Rama `HBM-9/scaffold-mock-server`, infraestructura Flutter, contrato
Prism, pruebas, gate de cobertura, commits atomicos y PR de HBM-7.

Decision coverage:
- D-01: usar exactamente las tres ramas `HBM-9/*` y un PR por ticket.
- D-02: mantener commits atomicos y PRs apilados HBM-7 -> HBM-8 -> HBM-9.
- D-03: exigir pruebas y cobertura changed-code >=80% en cada ticket.
- D-04: ejecutar analyze, test, build y Prism antes de push/PR.
- D-05: conservar feature-first, Riverpod, go_router, Dio y storage seguro.
- D-06: usar OpenAPI/Prism, zonas IANA y excluir features posteriores.
- D-07: respaldar, restaurar selectivamente y auditar el worktree combinado.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-identity-profile-contract-foundation/01-CONTEXT.md
@.planning/phases/01-identity-profile-contract-foundation/01-RESEARCH.md
@.planning/phases/01-identity-profile-contract-foundation/01-VALIDATION.md
@pubspec.yaml
@lib/app.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proteger el worktree y abrir la rama HBM-7</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/phases/01-identity-profile-contract-foundation/01-CONTEXT.md, .planning/phases/01-identity-profile-contract-foundation/01-RESEARCH.md</read_first>
  <action>Registrar el commit y `git status` iniciales. Crear un stash de seguridad nombrado `pre-phase-1-combined-worktree` que incluya tracked y untracked, excluyendo `.planning/**` para que los planes sigan disponibles. Verificar con `git stash list` y `git stash show --stat --include-untracked` que el respaldo contiene la implementacion combinada. Confirmar que fuera de `.planning` el worktree vuelve al baseline y crear desde `main` la rama exacta `HBM-9/scaffold-mock-server`. Guardar el identificador del stash en STATE. No usar reset hard, checkout destructivo ni eliminar el stash.</action>
  <verify>git branch --show-current; git stash list; git status --short</verify>
  <acceptance_criteria>
    - La rama activa se llama exactamente `HBM-9/scaffold-mock-server`.
    - `git stash list` contiene `pre-phase-1-combined-worktree`.
    - El stash incluye los archivos modificados y untracked observados antes de la limpieza.
    - Fuera de `.planning`, no quedan cambios de auth, perfil o features futuras.
  </acceptance_criteria>
  <done>El trabajo combinado esta respaldado y HBM-7 parte de un baseline verificable.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruir el scaffold, core y mock de HBM-7</name>
  <files>.gitignore, README.md, analysis_options.yaml, bitbucket-pipelines.yml, docs/openapi.yaml, lib/app.dart, lib/main.dart, lib/core/config/app_env.dart, lib/core/network/api_exception.dart, lib/core/network/auth_session_controller.dart, lib/core/network/dio_client.dart, lib/core/network/jwt_interceptor.dart, lib/core/router/app_router.dart, lib/core/router/app_routes.dart, lib/core/storage/token_storage.dart, lib/core/theme/app_theme.dart, lib/features/auth/README.md, lib/features/goals/README.md, lib/features/habits/README.md, lib/features/profile/README.md, lib/features/progress/README.md, lib/features/reminders/README.md, lib/features/tracking/README.md, package.json, package-lock.json, pubspec.yaml, pubspec.lock, scripts/bootstrap.ps1, scripts/mock-smoke.mjs</files>
  <read_first>docs/openapi.yaml, pubspec.yaml, lib/core/network/api_client.dart, lib/core/storage/secure_token_storage.dart, bitbucket-pipelines.yml</read_first>
  <action>Recuperar selectivamente desde el stash solo infraestructura y documentacion de HBM-7, revisando el contenido antes de agregarlo. Configurar Riverpod codegen, go_router, Dio y flutter_secure_storage; reemplazar las abstracciones antiguas de red/storage sin dejar imports rotos. Implementar AppEnv, ApiException, DioClient, JwtInterceptor, AuthSessionController, TokenStorage, router minimo y theme base. Mantener auth y profile como limites de feature documentados, no como pantallas funcionales. Excluir las implementaciones data/domain/presentation de goals, habits, reminders, tracking y progress. Completar `docs/openapi.yaml` con los endpoints y schemas de Phase 1, y configurar Prism, bootstrap y pipeline. Ejecutar codegen con Flutter 3.44.x. Separar commits por responsabilidad, como `build(HBM-7): configure Flutter dependencies and tooling`, `feat(HBM-7): add secure networking foundation` y `docs(HBM-7): add executable Phase 1 API contract`.</action>
  <verify>powershell -ExecutionPolicy Bypass -File scripts/bootstrap.ps1; flutter analyze; npm run mock:smoke; git diff main...HEAD --name-status</verify>
  <acceptance_criteria>
    - `pubspec.yaml` declara Riverpod codegen, go_router, Dio y flutter_secure_storage.
    - `lib/core/storage/token_storage.dart` usa FlutterSecureStorage y no SharedPreferences.
    - `lib/core/network/jwt_interceptor.dart` obtiene tokens desde TokenStorage y limpia sesion si refresh falla.
    - `docs/openapi.yaml` es aceptado por Prism e incluye registro, login, password reset y perfil.
    - No existen archivos funcionales bajo `lib/features/goals|habits|reminders|tracking|progress` aparte de sus README de limite.
    - `flutter analyze` termina con exit code 0.
  </acceptance_criteria>
  <done>El scaffold y mock de HBM-7 son reproducibles, seguros y no mezclan funcionalidades posteriores.</done>
</task>

<task type="auto">
  <name>Task 3: Probar, medir y publicar HBM-7</name>
  <files>scripts/check-changed-coverage.mjs, scripts/check-changed-coverage.test.mjs, test/core/network/api_exception_test.dart, bitbucket-pipelines.yml, .planning/phases/01-identity-profile-contract-foundation/01-01-SUMMARY.md, .planning/VERIFICATION.md</files>
  <read_first>lib/core/network/api_exception.dart, lib/core/network/jwt_interceptor.dart, scripts/mock-smoke.mjs, bitbucket-pipelines.yml, .planning/phases/01-identity-profile-contract-foundation/01-VALIDATION.md</read_first>
  <action>Agregar pruebas unitarias del core, incluyendo normalizacion de errores y comportamiento testeable de almacenamiento/interceptor. Implementar un parser de LCOV con pruebas que calcule cobertura para archivos Dart cambiados, excluya generado y falle por debajo de 80%. Integrar el gate al pipeline. Ejecutar la suite focalizada, analyze, cobertura, build web y smoke Prism. Revisar que `git diff main...HEAD` solo contenga HBM-7 y que no haya secretos. Crear commits atomicos de tests/CI y evidencia. Push de la rama y crear en Bitbucket un PR hacia `main` con titulo `HBM-7: Scaffold Flutter feature-first skeleton and mock server`, resumen, pruebas, porcentaje de cobertura y dependencia HBB-7. No incluir el token Jira ni credenciales en commit, logs o PR.</action>
  <verify>node --test scripts/check-changed-coverage.test.mjs; flutter test test/core --coverage; node scripts/check-changed-coverage.mjs --base main --lcov coverage/lcov.info --min 80; flutter analyze; flutter build web --release; npm run mock:smoke; git log --oneline main..HEAD</verify>
  <acceptance_criteria>
    - Las pruebas del parser cubren exclusion, fallo bajo 80% y exito en 80% o superior.
    - Las pruebas unitarias de HBM-7 pasan.
    - La cobertura del codigo cambiado de HBM-7 es al menos 80%.
    - Analyze, build web y smoke Prism pasan.
    - La rama remota `HBM-9/scaffold-mock-server` existe.
    - Bitbucket contiene un PR de HBM-7 hacia `main` con evidencia de pruebas y cobertura.
  </acceptance_criteria>
  <done>HBM-7 esta aislado, probado, publicado y listo para revision.</done>
</task>

</tasks>

<verification>
- [ ] El stash de seguridad permanece disponible.
- [ ] `git diff main...HEAD` corresponde unicamente a HBM-7.
- [ ] `flutter analyze` y las pruebas focalizadas pasan.
- [ ] La cobertura del codigo cambiado es >=80%.
- [ ] Build web y smoke Prism pasan.
- [ ] El PR de HBM-7 existe y apunta a `main`.
</verification>

<success_criteria>
- Todas las tareas y criterios de aceptacion estan completos.
- HBM-7 tiene varios commits atomicos y ningun secreto.
- La infraestructura soporta los planes HBM-8 y HBM-9 sin incluirlos.
- La evidencia y URL del PR estan en `01-01-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-profile-contract-foundation/01-01-SUMMARY.md`
</output>
