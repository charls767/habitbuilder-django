---
phase: 03-reminders
plan: "07"
ticket: HBM-14
type: execute
wave: 7
depends_on: ["03-06"]
branch: HBM-14/local-notifications
pr_base: main
working_directory: C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile-hbm14
files_modified:
  - lib/features/reminders/application/reminder_reconciliation_coordinator.dart
  - lib/features/reminders/presentation/providers/reminder_providers.dart
  - lib/features/reminders/presentation/screens/reminders_screen.dart
  - lib/features/reminders/presentation/widgets/reminder_reconciliation_bootstrap.dart
  - lib/features/habits/presentation/providers/habit_providers.dart
  - lib/features/profile/presentation/providers/profile_providers.dart
  - lib/app.dart
  - test/features/reminders/application/reminder_reconciliation_test.dart
  - test/features/reminders/presentation/reminder_delivery_state_test.dart
  - test/features/habits/presentation/habit_lifecycle_test.dart
  - test/features/profile/presentation/profile_controller_test.dart
  - test/app_test.dart
autonomous: true
requirements: [REMINDER-02, REMINDER-03, REMINDER-05, QUALITY-14, QUALITY-16, QUALITY-17]
decisions: [D-06, D-08, D-09, D-10, D-11, D-12, D-13, D-15]
must_haves:
  truths:
    - "Complete desired state is computed before any cancellation; partial inputs leave prior schedules untouched."
    - "Only `habit-reminder:v1:` pending entries are replaced; unrelated notifications survive and cancelAll is absent."
    - "Reminder, habit, profile, authenticated startup/login and resume triggers serialize/coalesce reconciliation."
    - "Denied/inexact/no-op delivery remains visible without reverting server configuration."
  artifacts:
    - path: "lib/features/reminders/application/reminder_reconciliation_coordinator.dart"
      provides: "serialized complete desired-state projection"
    - path: "lib/features/reminders/presentation/widgets/reminder_reconciliation_bootstrap.dart"
      provides: "authenticated startup/resume trigger owner"
    - path: "test/features/reminders/application/reminder_reconciliation_test.dart"
      provides: "fail-safe/managed/serialization/trigger evidence"
    - path: "test/features/reminders/presentation/reminder_delivery_state_test.dart"
      provides: "degraded delivery UI evidence"
  key_links:
    - from: "ReminderReconciliationCoordinator"
      to: "ReminderScheduler"
      via: "replaceManaged only after complete desired set"
      pattern: "replaceManaged"
    - from: "HabitBuilderApp"
      to: "ReminderReconciliationBootstrap"
      via: "authenticated lifecycle composition"
      pattern: "ReminderReconciliationBootstrap"
---

<objective>
Reconcile backend/profile state into managed local notifications and wire every
D-12 trigger.

Purpose: Prevent partial destructive updates, stale schedules and false
permission success.
Output: Coordinator, lifecycle bootstrap, controller integrations, degraded
state UI and deterministic tests.
</objective>

<execution_context>
@C:/Users/USER/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reminders/03-HBM-14-BASE.json
@C:/Users/USER/Desktop/DPPF/HabitBuilder/habitbuilder-mobile/.planning/phases/03-reminders/03-06-SUMMARY.md
@lib/features/reminders/domain/services/reminder_scheduler.dart
@lib/features/reminders/infrastructure/notifications/reminder_scheduler_factory.dart

<guard>Run only in the isolated root, assert base/root/branch plus primary D-15 values before/after each task, and run the tested package post-gate plus boundary scans in every verify.</guard>
</context>

## Artifacts

- `ReminderReconciliationCoordinator`: serialized/coalesced request queue,
  complete input fetch, eligibility projection, `replaceManaged` and
  `ReminderDeliveryState`.
- `ReminderReconciliationBootstrap`: auth startup/login and app-resume owner
  without provider-build side effects.
- Trigger wiring after successful reminder CRUD/toggle, habit
  pause/resume/complete and profile zone/preferences updates.
- Screen status for denied, inexact and unsupported no-op delivery with saved
  reminder configuration intact.
- Tests for partial fetch, invalid zone, managed/unrelated pending entries,
  coalescing and every trigger.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement serialized fail-safe desired-state reconciliation</name>
  <read_first>
    <file>.planning/phases/03-reminders/03-CONTEXT.md — D-08/D-09/D-12</file>
    <file>lib/features/reminders/domain/services/reminder_scheduler.dart — replace contract</file>
    <file>lib/features/reminders/domain/services/reminder_occurrence_planner.dart — pure desired occurrences</file>
    <file>lib/features/profile/domain/entities/perfil_usuario.dart — zone/preferences</file>
    <file>scripts/check-reminder-boundaries.mjs — mandatory scans</file>
  </read_first>
  <files>lib/features/reminders/application/reminder_reconciliation_coordinator.dart, test/features/reminders/application/reminder_reconciliation_test.dart</files>
  <action>Write failing tests for preferences off, inactive habits/reminders, invalid zone, partial fetch, managed/unrelated pending entries, serialized overlap and one coalesced rerun. Implement the coordinator so all profile/habit/reminder inputs and occurrences complete before any scheduler mutation; any failure leaves prior schedules untouched. Filter only literal `habit-reminder:v1:` entries and never call `cancelAll`. Use injected profile `tz.Location`/clock only. Commit exact paths as `feat(HBM-14): add fail-safe reminder reconciliation`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test test/features/reminders/application/reminder_reconciliation_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze &amp;&amp; node scripts/verify-reminder-package-gate.mjs post --evidence .planning/phases/03-reminders/03-HBM-14-PACKAGES.json --pubspec pubspec.yaml --lockfile pubspec.lock &amp;&amp; node scripts/check-reminder-boundaries.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Failed/partial inputs cause zero scheduler mutation.
    - Managed replacement preserves unrelated pending notifications and serializes overlaps.
  </acceptance_criteria>
  <done>Reconciliation is a complete, serialized and non-destructive projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire every D-12 trigger and degraded delivery state</name>
  <read_first>
    <file>lib/features/reminders/application/reminder_reconciliation_coordinator.dart — Task 1 API</file>
    <file>lib/features/reminders/presentation/providers/reminder_providers.dart — reminder success paths</file>
    <file>lib/features/habits/presentation/providers/habit_providers.dart — lifecycle success paths</file>
    <file>lib/features/profile/presentation/providers/profile_providers.dart — profile/logout paths</file>
    <file>lib/app.dart — auth/router composition</file>
  </read_first>
  <files>lib/features/reminders/presentation/providers/reminder_providers.dart, lib/features/reminders/presentation/screens/reminders_screen.dart, lib/features/reminders/presentation/widgets/reminder_reconciliation_bootstrap.dart, lib/features/habits/presentation/providers/habit_providers.dart, lib/features/profile/presentation/providers/profile_providers.dart, lib/app.dart, test/features/reminders/presentation/reminder_delivery_state_test.dart, test/features/habits/presentation/habit_lifecycle_test.dart, test/features/profile/presentation/profile_controller_test.dart, test/app_test.dart</files>
  <action>Write failing tests for every D-12 trigger. Reconcile only after successful reminder create/edit/reactivate/deactivate/delete, habit pause/resume/complete, profile zone/preferences update, authenticated startup/login and app resume. No scheduling/network side effect may occur during provider build. Surface denied/inexact/no-op states and retry/settings action without undoing server data or reporting delivery success. Guard and commit exact paths as `feat(HBM-14): wire reminder reconciliation triggers`.</action>
  <verify>
    <automated>C:\Users\USER\flutter\bin\flutter.bat test test/features/reminders/application/reminder_reconciliation_test.dart test/features/reminders/presentation/reminder_delivery_state_test.dart test/features/habits/presentation/habit_lifecycle_test.dart test/features/profile/presentation/profile_controller_test.dart test/app_test.dart &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat analyze &amp;&amp; C:\Users\USER\flutter\bin\flutter.bat build web --release &amp;&amp; node scripts/check-reminder-boundaries.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Partial fetch/invalid zone produces zero cancellation/schedule mutation.
    - Every D-12 trigger is asserted and rapid requests serialize/coalesce.
    - Denied/degraded/unsupported state preserves configured reminders.
  </acceptance_criteria>
  <done>Device scheduling stays synchronized through every locked integration event.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Partial destructive reconciliation | mitigate | Complete inputs before replace |
| Duplicate schedule races | mitigate | Serialized/coalesced coordinator |
| Unrelated notification loss | mitigate | Versioned managed prefix; cancelAll gate |
| False permission success | mitigate | Explicit delivery state |
| Local-zone drift | mitigate | Profile Location plus injected clock |
</threat_model>

<verification>Focused/full integration, web and all boundary scans pass in the isolated root.</verification>
<success_criteria>Every D-12 trigger updates only the managed projection without losing configured data.</success_criteria>
<output>Write and locally version `03-07-SUMMARY.md` from the primary root; do not push metadata.</output>
